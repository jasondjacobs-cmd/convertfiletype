(() => {
  const input=document.querySelector('[data-bmp-file]'); if(!input)return;
  const dropZone=document.querySelector('[data-drop-zone]'),selected=document.querySelector('[data-selected-file]'),fileName=document.querySelector('[data-file-name]'),fileSize=document.querySelector('[data-file-size]'),clearButton=document.querySelector('[data-clear-file]'),quality=document.querySelector('[data-quality]'),qualityOutput=document.querySelector('[data-quality-output]'),convertButton=document.querySelector('[data-convert]'),status=document.querySelector('[data-status]'),progress=document.querySelector('[data-progress]'),progressLabel=document.querySelector('[data-progress-label]'),progressPercent=document.querySelector('[data-progress-percent]'),progressTrack=document.querySelector('[data-progress-track]'),progressBar=document.querySelector('[data-progress-bar]'),progressDetail=document.querySelector('[data-progress-detail]'),result=document.querySelector('[data-result]'),resultSize=document.querySelector('[data-result-size]'),preview=document.querySelector('[data-preview]'),download=document.querySelector('[data-download]'),another=document.querySelector('[data-convert-another]');
  const MAX_BYTES=25*1024*1024,CONVERSION_TIMEOUT_MS=10000,PREVIEW_TIMEOUT_MS=5000; let currentFile=null,resultUrl=null,runId=0,converting=false,startedAt=0;
  const formatBytes=bytes=>{if(!Number.isFinite(bytes)||bytes<=0)return'0 B';const units=['B','KB','MB','GB'],i=Math.min(Math.floor(Math.log(bytes)/Math.log(1024)),units.length-1),v=bytes/Math.pow(1024,i);return`${v>=10||i===0?v.toFixed(0):v.toFixed(1)} ${units[i]}`;};
  const elapsedMs=()=>performance.now()-startedAt,elapsed=()=>`${(elapsedMs()/1000).toFixed(1)}s elapsed`;
  const setProgress=(percent,label,detail='')=>{const v=Math.max(0,Math.min(100,percent));progress.hidden=false;progressLabel.textContent=label;progressPercent.textContent=`${v}%`;progressTrack.setAttribute('aria-valuenow',String(v));progressBar.style.width=`${v}%`;progressDetail.textContent=detail;};
  const hideProgress=()=>{progress.hidden=true;progressBar.style.width='0%';progressTrack.setAttribute('aria-valuenow','0');progressDetail.textContent='';}; const setIdleButton=()=>{converting=false;convertButton.textContent='Convert to JPG';convertButton.disabled=!currentFile;};
  const revokeResult=()=>{if(resultUrl)URL.revokeObjectURL(resultUrl);resultUrl=null;preview.removeAttribute('src');download.removeAttribute('href');download.removeAttribute('download');result.removeAttribute('data-conversion-elapsed-ms');resultSize.textContent='';result.hidden=true;};
  const reset=()=>{runId++;currentFile=null;input.value='';selected.hidden=true;revokeResult();hideProgress();status.textContent='Choose a file to begin.';setIdleButton();};
  const isBmp=file=>file.type==='image/bmp'||file.type==='image/x-ms-bmp'||/\.bmp$/i.test(file.name);
  const selectFile=file=>{runId++;revokeResult();hideProgress();if(!file)return reset();if(!isBmp(file)){reset();status.textContent='Please choose a BMP image.';return;}if(file.size>MAX_BYTES){reset();status.textContent='That file is larger than the 25 MB limit.';return;}currentFile=file;fileName.textContent=file.name;fileSize.textContent=formatBytes(file.size);selected.hidden=false;status.textContent='Ready to convert.';setIdleButton();};
  const withTimeout=(promise,ms,message)=>Promise.race([promise,new Promise((_,reject)=>window.setTimeout(()=>reject(new Error(message)),ms))]);
  const loadImage=url=>new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>image.naturalWidth>0&&image.naturalHeight>0?resolve(image):reject(new Error('BMP image has invalid dimensions.'));image.onerror=()=>reject(new Error('BMP could not be decoded by this browser.'));image.src=url;});
  const canvasToJpeg=(canvas,q)=>new Promise((resolve,reject)=>canvas.toBlob(blob=>blob&&blob.size?resolve(blob):reject(new Error('JPG encoding failed.')),'image/jpeg',q));
  input.addEventListener('change',()=>selectFile(input.files?.[0]));clearButton.addEventListener('click',reset);another.addEventListener('click',reset);quality.addEventListener('input',()=>qualityOutput.textContent=`${quality.value}%`);['dragenter','dragover'].forEach(n=>dropZone.addEventListener(n,e=>{e.preventDefault();dropZone.classList.add('is-dragging');}));['dragleave','drop'].forEach(n=>dropZone.addEventListener(n,e=>{e.preventDefault();dropZone.classList.remove('is-dragging');}));dropZone.addEventListener('drop',e=>selectFile(e.dataTransfer?.files?.[0]));
  convertButton.addEventListener('click',async()=>{if(!currentFile||converting)return;const activeRun=++runId,sourceFile=currentFile;startedAt=performance.now();converting=true;convertButton.disabled=true;convertButton.textContent='Converting…';revokeResult();try{const conversionPromise=(async()=>{setProgress(10,'Reading BMP file',`${formatBytes(sourceFile.size)} source file`);status.textContent='Reading your BMP on this device…';const sourceUrl=URL.createObjectURL(sourceFile);let image;try{setProgress(30,'Decoding BMP',elapsed());image=await loadImage(sourceUrl);}finally{URL.revokeObjectURL(sourceUrl);}if(activeRun!==runId||sourceFile!==currentFile)throw new Error('Conversion cancelled.');setProgress(55,'Preparing image',`${image.naturalWidth} × ${image.naturalHeight} · ${elapsed()}`);const canvas=document.createElement('canvas');canvas.width=image.naturalWidth;canvas.height=image.naturalHeight;const context=canvas.getContext('2d',{alpha:false});if(!context)throw new Error('Canvas is unavailable in this browser.');context.fillStyle='#ffffff';context.fillRect(0,0,canvas.width,canvas.height);context.drawImage(image,0,0);setProgress(72,'Encoding JPG',`${quality.value}% quality · ${elapsed()}`);const jpgBlob=await canvasToJpeg(canvas,Number(quality.value)/100);const type=(jpgBlob.type||'').toLowerCase();if(type!=='image/jpeg'&&type!=='image/jpg')throw new Error(`Unexpected output type: ${jpgBlob.type||'unknown'}`);const bytes=new Uint8Array(await jpgBlob.slice(0,3).arrayBuffer());if(bytes[0]!==0xff||bytes[1]!==0xd8||bytes[2]!==0xff)throw new Error('Generated JPG signature is invalid.');setProgress(88,'Validating JPG',`${formatBytes(jpgBlob.size)} output · ${elapsed()}`);const candidateUrl=URL.createObjectURL(jpgBlob);let validated;try{validated=await withTimeout(loadImage(candidateUrl),PREVIEW_TIMEOUT_MS,'Preview timed out.');}catch(error){URL.revokeObjectURL(candidateUrl);throw error;}if(validated.naturalWidth!==image.naturalWidth||validated.naturalHeight!==image.naturalHeight){URL.revokeObjectURL(candidateUrl);throw new Error('Generated JPG dimensions do not match the BMP source.');}if(activeRun!==runId||sourceFile!==currentFile){URL.revokeObjectURL(candidateUrl);throw new Error('Conversion cancelled.');}setProgress(96,'Preparing download',`${validated.naturalWidth} × ${validated.naturalHeight} · ${elapsed()}`);resultUrl=candidateUrl;const baseName=sourceFile.name.replace(/\.bmp$/i,'')||'converted-image';preview.src=resultUrl;download.href=resultUrl;download.download=`${baseName}.jpg`;resultSize.textContent=`${formatBytes(jpgBlob.size)} JPG · ${validated.naturalWidth} × ${validated.naturalHeight}`;result.dataset.conversionElapsedMs=String(Math.round(elapsedMs()));result.hidden=false;setProgress(100,'Complete',`${elapsed()} total`);status.textContent='Conversion complete.';})();await withTimeout(conversionPromise,CONVERSION_TIMEOUT_MS,'Conversion timed out.');}catch(error){if(activeRun!==runId)return;revokeResult();console.error('BMP to JPG conversion failed',error);const message=error?.message||'Unknown conversion error';setProgress(100,'Conversion stopped',`${message} · ${elapsed()}`);status.textContent=message==='Conversion timed out.'?'Conversion took too long. This file could not be processed quickly enough on this device.':`Conversion failed: ${message}`;}finally{if(activeRun===runId)setIdleButton();}});
  window.addEventListener('pagehide',()=>{runId++;revokeResult();});
})();